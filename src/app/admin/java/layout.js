 

export const metadata = {
  title: 'Java Executor - Next.js App Router',
  description: 'Execute Java programs from your web browser',
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}